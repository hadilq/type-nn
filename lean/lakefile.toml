name = "TypeNN"
version = "0.1.0"
defaultTargets = ["TypeNN"]

# No Mathlib. Every proof in this library uses only the axioms of a linearly
# ordered commutative ring, so bare Lean core is enough and `lake build`
# finishes in seconds instead of fetching a multi-gigabyte cache.

[[lean_lib]]
name = "TypeNN"
